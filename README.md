# classification-pipeline
Repository for Lab 3 in the Project I course.

This repository contains all of the necessary source files for this project.  The `src/` folder contains the Python models that were used to generate the plots and figures, `figures/generated` holds all of the generated figures, and `report/` holds the source code and PDF file of the report written in LaTeX.
